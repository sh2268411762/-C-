/*
#include <stdio.h>
#include <stdlib.h>
int main()
{
	int number = 200;
	int year = 2014;
	do {
		year++;
		number = number * 1.2;                    //这里是不是应该写点什么？
	} while (number < 1000);     //使用do-while结构语句时，while括号后必须有分号。
			printf("到%d年招工规模突破1000人\n", year);
		return 0;
}
*/