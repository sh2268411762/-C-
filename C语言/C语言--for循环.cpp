/*
#include <stdio.h>
#include <stdlib.h>
int main()
{
	// 定义变量sum, num
	int sum, num;
	sum = 0;
	for (num = 0;num<=10;num++) //for循环条件与num的变化值
	{
		sum = sum + num;    //计算每次数字之间的和sum
	}
	printf("10以内数的和为：%d", sum);
	return 0;
}//      for循环中，表达式1是一个或多个赋值语句，它用来控制变量的初始值；
//       表达式2是一个关系表达式，它决定什么时候退出循环；
//       表达式3是循环变量的步进值，定义控制循环变量每循环一次后按什么方式变化。这三部分之间用分号(;)分开。
*/